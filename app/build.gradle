plugins {
    id 'com.android.application'
}

android {
    namespace 'io.github.madokatext.ecarxmod'
    // Compile against the actual head-unit API so newer framework calls cannot slip in.
    compileSdk 28
    buildToolsVersion '34.0.0'

    defaultConfig {
        applicationId 'io.github.madokatext.ecarxmod'
        minSdk 28
        targetSdk 28
        versionCode 9
        versionName '1.5.1'
    }

    buildTypes {
        debug {
            minifyEnabled false
        }
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    // API 82 supplies the legacy Java declarations; LSPosed API 93 supplies them at runtime.
    compileOnly 'de.robv.android.xposed:api:82'
}
